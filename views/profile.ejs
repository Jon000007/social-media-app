<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../stylesheets/profile.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title><%=user.username%>'s Gallery</title>
</head>

<body>
    <div class="main-profile">
        <div class="logo">
            <img src="../images/logo.png" alt="Profile Image" srcset="">
        </div>
        <h2>
            <%= user.username %>
        </h2>
        <a type="button" href="/logout" id="logout-btn" onclick="logout()">Logout</a>
        <form action="/upload" method="post" enctype="multipart/form-data">
            <input type="text" name="caption" placeholder="Write Caption">
            <input type="file" name="file">
            <button type="submit">Upload</button>
        </form>
    </div>
    <hr>
    <h3>Posts</h3>
    <div class="posts">
        <% posts.forEach(function(post) { %>
            <div class="post">
                <div class="image">
                    <img src="/images/uploads/<%= post.image %>">
                    <a href="/delete/<%= post._id %>"><i class="fa-solid fa-trash"></i></a>
                </div>
                <p>
                    <%= post.caption %>
                </p>
            </div>
        <% }) %>
    </div>
    <script src="../javascripts/script.js"></script>
</body>

</html>
